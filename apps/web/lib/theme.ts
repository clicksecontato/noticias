/**
 * Design tokens do projeto (referência).
 * Cores e espaçamentos padronizados; uso principal via variáveis CSS em globals.css.
 */

export const theme = {
  /** Largura máxima do conteúdo principal */
  contentMaxWidth: "960px",
  /** Altura da barra de navegação */
  navHeight: "56px",
  /** Raio de borda padrão (cards, inputs, botões) */
  radius: "0.75rem",
} as const;
