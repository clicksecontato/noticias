{
  "name": "noticias-gaming-platform",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build:web": "npm run build --workspace web",
    "start:web": "npm run start --workspace web",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:db": "vitest run packages/database/tests",
    "test:seo": "vitest run packages/seo/tests"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.0"
  }
}
